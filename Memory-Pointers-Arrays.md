# 内存、指针与数组 — 从零讲透

> 关联学习：Cpp-Symbol-Reference.md 中的指针相关符号

---

## 第一章：你的程序怎么用内存

### 1.1 什么是内存？

内存可以想象成一个**巨大的柜子**，每个抽屉有一个**编号**（地址）和**容量**（8 个字节 = 一个 double 的大小）：

```
内存柜子示意图（简化）：
┌──────────┬──────────┬──────────┬──────────┐
│ 地址 0   │ 地址 1   │ 地址 2   │ 地址 3   │  ...→
├──────────┼──────────┼──────────┼──────────┤
│ 数据     │ 数据     │ 数据     │ 数据     │
│ 8字节    │ 8字节    │ 8字节    │ 8字节    │
└──────────┴──────────┴──────────┴──────────┘
```

当你写 `double x = 42.0;` 时，C++ 就帮你在这个柜子里找了一个空抽屉，把 42.0 放进去，并且记下了这个抽屉的编号。

### 1.2 变量就是"有名字的抽屉"

```cpp
double x = 42.0;
```

实际上发生了什么：

```
x 这个变量 → 对应一个抽屉
抽屉编号（地址）= 比如 0x7ffe1234
抽屉里装的内容 = 42.0（8 个字节）
```

把变量名想象成**贴在抽屉上的标签**——你不需要记抽屉编号，直接喊名字就能找到。

---

## 第二章：指针——变量存的是地址而不是数值

### 2.1 普通变量 vs 指针变量

```cpp
double value = 42.0;      // 普通变量：抽屉里放 42.0
double* ptr = &value;      // 指针变量：抽屉里放 value 的地址
```

图解：

```
普通变量 value:
┌──────────────────┐
│     42.0         │  ← 抽屉里放的是数值
│ 地址: 0x100      │
└──────────────────┘
        ↑
        │
指针变量 ptr:
┌──────────────────┐
│     0x100        │  ← 抽屉里放的是地址（另一个抽屉的门牌号）
│ 地址: 0x200      │
└──────────────────┘
```

**两者都是变量，区别在于存的内容类型不同：**
- `double value` — 存一个浮点数
- `double* ptr` — 存一个**指向 double 的地址**（门牌号）

### 2.2 & 符号：取地址

`&value` 的意思是"value 这个抽屉的编号是多少？"

```cpp
std::cout << &value;   // 输出类似 0x7ffe1234（一串十六进制数）
```

不管你在代码里写了多少变量，**每个变量都有一个唯一的地址**。& 就是问"你住在几号？"

### 2.3 * 符号：解引用

```cpp
double* ptr = &value;   // ptr 里存的是 0x100（value 的门牌号）
std::cout << *ptr;      // *ptr 的意思："去 0x100 这个地址看看里面有什么"
                        // 输出 42.0
```

`*ptr` 可以理解为：
1. 翻开 ptr 的抽屉，看到门牌号 0x100
2. 走到 0x100 那个抽屉前
3. 打开它，读出里面的内容 42.0

### 2.4 什么时候用指针？

直接看一个对比：

```cpp
// 没有指针：我复制一份给你
double a = 42.0;
double b = a;      // b = 42.0，a 和 b 是独立的两个抽屉
b = 100.0;         // a 还是 42.0，b 变成了 100.0

// 用指针：我告诉你我的地址
double a = 42.0;
double* b = &a;    // b 存的是 a 的地址，不是数值
*b = 100.0;        // 通过地址修改 a 的内容
std::cout << a;    // 输出 100.0！a 被改了
```

**指针让你间接地操作另一个变量。** 为什么需要这种"间接"？后面讲到函数传参和动态内存时会看到。

---

## 第三章：数组——连续排列的抽屉

### 3.1 数组的实质

```cpp
double arr[3] = {1.0, 2.0, 3.0};
```

这句话在内存里干了什么？**一口气申请了 3 个连续排列的抽屉：**

```
arr:
┌──────┬──────┬──────┐
│ 1.0  │ 2.0  │ 3.0  │
│0x100 │0x108 │0x110 │
└──────┴──────┴──────┘
↑
arr 就是 0x100
```

关键：每个 double 占 8 字节，所以地址依次相差 8：
- arr[0] 在 0x100
- arr[1] 在 0x108（0x100 + 1×8）
- arr[2] 在 0x110（0x100 + 2×8）

### 3.2 数组名就是地址

```cpp
std::cout << arr;       // 输出 0x100（首地址）
std::cout << &arr[0];   // 也输出 0x100
std::cout << arr[0];    // 输出 1.0
```

**数组名 arr 等价于 &arr[0]**，就是第一个元素的地址。

**所以下面的写法完全等价：**

```cpp
arr[1]         // 常用写法，人话友好
*(arr + 1)     // 指针写法，底层原理
```

`arr + 1` 不是地址加 1，而是加一个 double 的长度（8 字节）。编译器看见 `arr` 是 `double*` 类型，自动帮你算好了。

### 3.3 指针和数组的等价性

```cpp
double arr[3] = {1.0, 2.0, 3.0};
double* p = arr;           // p 指向 arr 的第一个元素

// 下面四种写法完全等价：
arr[0]   → 1.0
*p       → 1.0
arr[1]   → 2.0
*(p + 1) → 2.0
p[1]     → 2.0   ← 指针也可以用 []！
arr[2]   → 3.0
*(p + 2) → 3.0
```

**所以数组本质上就是指针**（准确说，数组名在表达式中会退化成指针）。这就是为什么 `p[i]` 和 `arr[i]` 都可以用。

### 3.4 一个重要的区别

虽然数组名可以当指针用，但下面两个有区别：

```cpp
double arr[3] = {1.0, 2.0, 3.0};
double* p = arr;

p = p + 1;      // 可以：p 现在指向 arr[1]
// arr = arr + 1;  // 不行！数组名是常量，不能修改
```

**数组名像常量指针**——内容不能改。你没办法让 arr 指向别处。

---

## 第四章：栈 vs 堆——两种不同的内存区域

这是理解内存分配最关键的一节。

### 4.1 栈（Stack）

**特点：** 小（几 MB），自动管理，"用完即丢"

```cpp
void func() {
    int a = 10;          // 栈：自动分配
    double arr[100];     // 栈：也是自动分配
}   // 到这里，a 和 arr 自动消失
```

**栈的工作原理（想象一叠盘子）：**

```
函数 main() 开始：
┌──────────┐
│ main 的  │  ← 放了一个盘子
│ 局部变量 │
└──────────┘

函数 func() 被调用：
┌──────────┐
│ func 的  │  ← 又放了一个盘子
│ 局部变量 │
├──────────┤
│ main 的  │
│ 局部变量 │
└──────────┘

func() 返回：
┌──────────┐
│ main 的  │  ← func 的盘子被拿走了
│ 局部变量 │
└──────────┘
```

**栈的优点：** 快，不用你操心释放
**栈的缺点：**
- 空间小（默认 1~8 MB）
- 大数组会栈溢出
- 大小必须在编译时确定

```cpp
// 危险的写法（栈可能溢出）：
double big_arr[1000000];   // 8 MB，超过默认栈大小 → 崩溃

// 安全的写法（运行时决定大小）：
int n;
std::cin >> n;
double* big_arr = new double[n];  // 不占用栈，放在堆上
```

### 4.2 堆（Heap，也称自由存储区）

**特点：** 大（几 GB），手动管理，灵活

```cpp
int* p = new int(42);     // 堆上分配一个 int，值为 42
double* q = new double[1000];  // 堆上分配 1000 个 double
delete p;                  // 手动释放
delete[] q;                // 手动释放（数组用 delete[]）
```

**堆的工作原理（想象停车场的空车位）：**

```
堆：
┌────┬────┬────┬────┬────┬────┬────┬────┐
│ 空 │ 用 │ 用 │ 空 │ 用 │ 空 │ 空 │ 用 │  ← 不连续
└────┴────┴────┴────┴────┴────┴────┴────┘
         ↑你要用就找一块空位
         ↓
new double[100] → 找到一块连续的空位
用完后 delete 释放，别人才能再用
```

**堆的优点：**
- 空间大（取决于系统内存 + 虚拟内存）
- 大小可以在运行时决定
- 可以动态增长

**堆的缺点：**
- 慢（需要找空位、管理碎片）
- 必须手动释放，否则**内存泄漏**

### 4.3 对比表格

| | 栈 | 堆 |
|---|---|---|
| 大小 | 1~8 MB（很小） | 几 GB（很大） |
| 速度 | 极快 | 较慢 |
| 管理 | 自动（函数结束就释放） | 手动（new/delete） |
| 大小决定 | 编译时确定 | 运行时确定 |
| 典型用途 | 局部变量、函数参数 | 大型数组、对象 |

### 4.4 图例：完整的内存布局

```
进程的内存空间（一个程序跑起来后）：
┌──────────────────────┐  高地址
│        栈            │  ← 局部变量、函数调用
│   （向下增长）        │
├──────────────────────┤
│                      │
│        堆            │  ← new/delete 分配的地方
│   （向上增长）        │
├──────────────────────┤
│    全局/静态变量      │
├──────────────────────┤
│     代码区           │  ← 你的程序指令
│     (.text)          │
└──────────────────────┘  低地址
```

---

## 第五章：内存泄漏——为什么必须 delete

### 5.1 什么是内存泄漏

```cpp
void leak_func() {
    double* data = new double[1000000];  // 堆上分配了 8 MB
    // 用了但忘记 delete
}   // 函数结束了，栈上的 data 指针没了
    // 但堆上的 8 MB 数据还在，没人能访问它了
    // 这 8 MB 直到程序结束才能被回收
```

**内存泄漏的本质：** 你**丢了钥匙**。堆上的数据还在，但存地址的那个指针变量已经不存在了（栈变量被自动回收），没人知道那段内存的地址，所以再也无法 delete。

图解：

```
第一步：int* p = new int;
栈：                   堆：
┌──────────┐          ┌──────────┐
│ p: 0x100 │──────→   │  ????    │
└──────────┘          │ (0x100)  │
                      └──────────┘

第二步：函数结束，p 被回收
栈：                   堆：
(空了)                ┌──────────┐
                      │  ????    │
                      │ (0x100)  │
                      └──────────┘
                      没人知道 0x100 这个地址了
                      这块内存永远无法释放！
```

### 5.2 内存泄漏的后果

```cpp
// 一个泄漏积累的例子：
while (true) {
    double* data = new double[100000];  // 每次循环 0.8 MB
    // 忘记 delete
}
// 运行十几秒后：内存吃完，系统卡死，程序被 OS 杀掉
```

**实际危害：**
- 短程序（算完就退）：泄漏几十 MB 无所谓，OS 会回收
- 长时间运行的程序（服务器、仿真）：
  - 跑一天泄漏 1 MB，一个月就 30 MB
  - 如果每秒泄漏 1 MB，几分钟就崩

### 5.3 怎么保证不漏

**黄金法则：每次 new 都要对应一个 delete，且时机要对。**

```cpp
// 正确写法：
double* data = new double[n];
// 使用 data ...
delete[] data;   // 用完立刻释放
data = nullptr;  // 好习惯：释放后置空，防止再次访问
```

**更常见的问题：提前 return 导致泄漏**

```cpp
double* compute() {
    double* data = new double[1000];
    
    if (something_wrong) {
        return data;  // 错误！还没 delete 就返回
    }
    
    delete[] data;
    return nullptr;   // 正确路径释放了
}
```

修正版：
```cpp
double* compute() {
    double* data = new double[1000];
    
    if (something_wrong) {
        delete[] data;   // 先释放再返回
        return nullptr;
    }
    
    delete[] data;
    return nullptr;
}
```

### 5.4 现代 C++ 的解决方案（了解即可）

```cpp
#include <vector>
#include <memory>

// 1. vector：自动管理动态数组
std::vector<double> data(1000);  // 不用 new
// 用 data[i] 操作...
// 函数结束时自动释放

// 2. unique_ptr：自动管理单个对象
std::unique_ptr<double> p = std::make_unique<double>(42.0);
// 不用 delete，智能指针自动释放
```

**vector 的本质：** 封装了 `new[]` / `delete[]`，但帮你管理生命周期，不会忘记释放。你在函数中使用 `return data` 时，vector 会自动拷贝/移动，不泄漏。

---

## 第六章：综合例子——有限元场景

```cpp
#include <iostream>

int main() {
    // 场景：读取网格，不知道有多少节点
    int node_count = 1000;  // 假设运行时从文件读取
    
    // 栈上：小东西
    int elem_per_node = 4;        // 没问题，栈
    double tolerance = 1e-6;      // 没问题，栈
    
    // 堆上：大东西（节点坐标）
    double* x = new double[node_count];  // 8 KB
    double* y = new double[node_count];  // 8 KB
    
    // 初始化
    for (int i = 0; i < node_count; i++) {
        x[i] = i * 0.01;          // 数组语法，清晰
        y[i] = i * 0.02;
    }
    
    // 刚度矩阵（假设每节点 2 个自由度）
    // 注意：2*node_count 行、2*node_count 列——矩阵很大
    // 这里只是演示指针概念，实际用 std::vector 更好
    
    // 遍历计算
    for (int i = 0; i < node_count; i++) {
        std::cout << "节点 " << i << ": (" 
                  << x[i] << ", " << y[i] << ")" << std::endl;
    }
    
    // 必须释放！
    delete[] x;
    delete[] y;
    
    return 0;
}
```

---

## 快速记忆

| 概念 | 一句话总结 |
|------|-----------|
| 变量 | 有名字的抽屉 |
| 地址 & | 抽屉的门牌号 |
| 指针 *p | 抽屉里放的是另一个抽屉的门牌号 |
| 解引用 * | 去那个门牌号看看里面有什么 |
| 数组 arr | 一排连续排列的抽屉，arr 自己就是第一个抽屉的门牌号 |
| arr[n] | 等价于 *(arr + n)，跳到第 n 个抽屉 |
| 栈 | 自动分配、自动回收的小空间（函数局部变量） |
| 堆 | 手动分配、手动回收的大空间（new/delete） |
| 内存泄漏 | 丢了钥匙，再也拿不到那块内存了 |
| delete[] | new 数组后必须用这个，不是 delete |
